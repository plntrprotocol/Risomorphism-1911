# Cosmic Pyramid Full-Size Scene Example

Source image:
- `source/cosmic-pyramid.jpg`

This example tests a square, high-contrast illustration with:
- strong geometric architecture (pyramid + staircase)
- a small robed human figure
- dense stippled cosmic sky / nebula texture
- a bright focal star at the apex

## Why this example matters

It expands the showcase beyond portraits and avatars.

This scene stresses a different part of the pipeline:
- sharp perspective lines
- small foreground figure readability
- atmospheric, punctuated sky texture
- large dark-field / bright-structure contrast

## Still-image commands used (via ascii-pipeline CLI)

These commands use the pure-Python pipeline and are the recommended reproducible renders.

### Full-size stroke-clarity (high-contrast poster)

```bash
ascii-pipeline render-image \
  --input source/cosmic-pyramid.jpg \
  --preset stroke-clarity \
  --scale 4 \
  --out preset-renders/fullsize-stroke-clarity.txt \
  --preview-out preset-renders/fullsize-stroke-clarity.png \
  --diagnostics-out preset-renders/fullsize-stroke-clarity.json
```

### Full-size D30 dense (atmospheric linework)

```bash
ascii-pipeline render-image \
  --input source/cosmic-pyramid.jpg \
  --preset d30-dense \
  --scale 4 \
  --out preset-renders/fullsize-d30-dense.txt \
  --preview-out preset-renders/fullsize-d30-dense.png \
  --diagnostics-out preset-renders/fullsize-d30-dense.json
```

### Base-scale D30 (48×24 avatar-sized fallback)

```bash
ascii-pipeline render-image \
  --input source/cosmic-pyramid.jpg \
  --preset d30-dense \
  --out preset-renders/d30-dense.txt \
  --preview-out preset-renders/d30-dense.png \
  --diagnostics-out preset-renders/d30-dense.json
```

### Scale-8 variant (384×192 showcase, heavy)

```bash
ascii-pipeline render-image \
  --input source/cosmic-pyramid.jpg \
  --preset d30-dense \
  --scale 8 \
  --out preset-renders/scale8-d30-dense.txt \
  --preview-out preset-renders/scale8-d30-dense.png \
  --diagnostics-out preset-renders/scale8-d30-dense.json
```

### Scale-16 variant (768×384 poster, very heavy)

```bash
ascii-pipeline render-image \
  --input source/cosmic-pyramid.jpg \
  --preset d30-dense \
  --scale 16 \
  --out preset-renders/scale16-d30-dense.txt \
  --preview-out preset-renders/scale16-d30-dense.png \
  --diagnostics-out preset-renders/scale16-d30-dense.json
```

## Recommended showcase assets

Still:
- `preset-renders/fullsize-stroke-clarity.png` — strongest high-contrast poster artifact
- `preset-renders/fullsize-d30-dense.png` — strongest atmospheric linework variant
- `preset-renders/d30-dense.png` — avatar-sized deployment fallback (48×24)

Interpretation:
- `stroke-clarity 192x96` = bold strokes, maximal contrast, poster-optimized
- `d30-dense 192x96` = 180-glyph palette, edge-aware downsampling, atmospheric
- `d30-dense 48x24` = compact avatar-ready derivative, retains recognizability

All assets are generated by the commands above and committed to `preset-renders/`.
